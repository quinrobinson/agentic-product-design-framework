---
type: llm
weight: 2
---
- It gives a concrete duration in milliseconds for the modal, between 150 and 400 ms.
- The entrance uses an ease-out style curve (named or as a cubic-bezier), not ease-in.
- The modal scales from slightly below full size (roughly 0.9-0.98) with opacity, not from zero, and grows from the center.
- It includes a reduced-motion variant.
- It gives a Flutter implementation or Flutter-specific values (e.g. Curves, Duration, showGeneralDialog / transitions).
